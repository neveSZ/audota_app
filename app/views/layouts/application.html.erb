<!DOCTYPE html>
<html>
  <head>
    <title>AudotaApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <% if admin_signed_in? %>
      <%= link_to "Lista de pedidos", orders_path  %>
      <%= link_to "Sair", destroy_admin_session_path, method: :delete  %>
    <% else %>
      <%= link_to "Adminstrador", admin_session_path  %>
    <% end %>
    <%= link_to "Lista de animais", animals_path %>
    <%= link_to "Lista de favoritos", favoritos_path %>
    <%= yield %>
  </body>
</html>
